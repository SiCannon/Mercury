//using Rms.Domain.Infrastructure;
//using Rms.Domain.Service.EF;

namespace Mercury.Console.Test
{
    class RmsContextTests
    {
        public static void Test()
        {
            /*
            RmsStartup.Go();

            var svc = new SongService();

            foreach (var s in svc.Songs)
                System.Console.WriteLine(s.Title);
            */
        }

    }
}
